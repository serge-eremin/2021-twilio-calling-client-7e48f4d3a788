const ready = 'ready'
const error = 'error'
const disconnect = 'disconnect'
const incoming = 'incoming'
const cancel = 'cancel'
const offline = 'offline'
const connect = 'connect'

export {
    ready,
    error,
    disconnect,
    incoming,
    cancel,
    offline,
    connect,
}